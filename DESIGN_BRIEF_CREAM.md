# BEAN. 크림 라이트 리디자인 브리프

> 2026-08-05 @orchestrator 확정: 디자인 방향을 다크 로스트 → **크림 라이트**로 전환한다.
> 이 문서는 디자인 세션(별도 Claude Code/디자이너)에 넘기는 핸드오프다.
> 배경 맥락은 DESIGN_DIRECTION.md(기존 다크 규범 — 이번 전환으로 대체 예정), 제품 정의는 PRODUCT.md.

---

## 1. 제품 한 줄

**BEAN.** — 스페셜티 커피 전용 체크인·향미 평가·발견 플랫폼 ("Vivino for Specialty Coffee").
모바일 웹(PWA), 390px 기준. 타깃: 카페 투어하는 20~30대 스페셜티 커피인.

## 2. 왜 바꾸나 (기존의 실패 원인 — 반복 금지)

다크브라운 배경 + 앰버 액센트의 **단일 색상군**이 문제였다. 세 번의 피드백:
"우울하다" → 글래스모피즘 → "너무 어둡다" → 밝기 상승 → "디자인·색상이 별로다".
톤 조절로는 해결 안 됨 — **갈색 위에 갈색**이라 탁하고 위계가 안 생긴다.

## 3. 새 방향: 크림 라이트

**"오전 11시의 밝은 스페셜티 카페 인테리어."** 화이트오크 테이블, 크림 벽, 테라코타 포인트.
Vivino·Untappd처럼 라이트 기반 — 콘텐츠(사진·차트·데이터)가 주인공이 되는 바탕.

### 시작점 팔레트 (목업 검증본 — 다듬어도 되지만 인상은 유지)

| 역할 | 값 | 비고 |
|---|---|---|
| 페이지 배경 | `#F2EAE0` | 웜 크림 — 순백 금지 |
| 카드 서피스 | `rgba(255,255,255,.75)` + 보더 `rgba(36,24,17,.10)` | 소프트 섀도 `0 8px 24px rgba(80,52,25,.10)` |
| 주 텍스트 | `#241811` | 에스프레소 |
| 보조 텍스트 | `#7A6A5C` | 라떼 그레이 |
| **액센트** | `#C4622D` | 테라코타 — 별점·CTA·활성 상태. **단일 액센트 원칙** |
| 보더/디바이더 | `#DCCFC0` | |
| CTA 텍스트 | `#FFFDF9` | 테라코타 버튼 위 |

주의: 크림≠파스텔. 채도 빠진 분홍/하늘/민트 금지(기존 원칙 유지). 화이트 순백(#FFF 전면) 금지 — 항상 웜톤.

### 유지되는 것 (변경 금지)

- **시그니처 2개**: ① 히어로 회전 원두(라이트 배경 버전으로 재해석 필요 — §5), ② 향미 레이더 차트(테라코타 스트로크로)
- 워드마크 "BEAN." 투톤+언더라인 규칙 (DESIGN_DIRECTION §2)
- 폰트: 디스플레이 세리프(제목) + Pretendard(본문) + mono(숫자·평점) 조합
- 모션: pressable(active scale .98), stagger 리스트 진입, prefers-reduced-motion 대응
- 한 화면 = 한 CTA. 체크인 3탭 원칙

## 4. 디자인해야 할 화면 (우선순위순)

1. **/map 홈** — 발견 진입점 2카드 + 카페 리스트(49곳, 평점/수집중 상태 혼재)
2. **/bean 원두 상세** — 원산지·컵노트·레이더(또는 "평가 수집 중")·향미 태그 칩·체크인 CTA·공식몰 구매 버튼(아웃라인)
3. **/checkin 스텝3 평가 화면** — 원두 선택·별점·5축 슬라이더(미평가 "–" 상태 있음)·태그 18개 피커·메모
4. **공유카드 9:16** — 인스타 스토리용. 레이더+별점+원두명+워드마크. *유일하게 다크 유지 검토 가능* (스토리에서 다크가 더 드라마틱 — 시안으로 판단)
5. /cafe 상세, /diary(취향 레이더+타임라인), /best 랭킹, 히어로 랜딩

## 5. 히어로 특별 지시

현재 히어로는 다크 배경에 3D 원두 포스터(이미지로 구움). 크림 배경 버전 컨셉 필요:
크림 바탕 + 원두의 진한 브라운이 대비로 살아나는 구도. 태그라인 "그 한 잔, 기억되게." 유지.

## 6. 돌려줄 산출물 (이 형식이면 바로 코드 반영 가능)

1. **확정 컬러 토큰 표** (hex — 위 표 형식 그대로, 역할별)
2. **주요 화면 시안 4장+**: 홈 / 원두 상세 / 체크인 평가 / 공유카드 (이미지 or HTML/CSS or Figma)
3. 히어로 라이트 버전 컨셉 1장
4. (선택) 컴포넌트 규칙 — 카드 radius/섀도, 칩, 버튼 상태

이미지만 와도 된다 — 구현 세션(이쪽)에서 토큰·컴포넌트로 번역한다.

## 7. 금지 (전 화면 공통 — CLAUDE.md §6)

파스텔 톤 / 템플릿 디폴트 룩 / "AI가 분석했습니다"류 카피 / 동물 이모지 / 순검정·순백 전면 사용

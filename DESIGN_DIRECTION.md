# BUNNA — DESIGN_DIRECTION.md

> 모든 시각 결정의 단일 출처. frontend-design + ui-ux-pro-max skill을 로드한 세션에서만 이 문서 관련 작업을 한다.
> 원칙: 볼드함은 한 곳에 쓴다. 이 제품의 시그니처는 두 개 — ①히어로의 회전 원두 ②레이더 차트. 나머지는 조용하고 절제되게.

---

## 1. 무드 정의 (2026-08-13 개정)
**"아침의 로스터리"**. 오전 햇살이 든 로스터리 — 화이트오크 작업대, 크림 벽, 종이 질감, 그 위에 놓인 진한 로스팅 원두. 밝지만 가볍지 않다: 밀도는 **색이 아니라 재질과 대비**로 만든다.

> 이전 무드였던 "밤의 로스터리"(다크 프리미엄)는 폐기했다. 근거는 §2 색 섹션 상단 참조 — 다크 자체보다 갈색 위 갈색의 위계 부재가 문제였고, 3회의 사용자 피드백으로 확인됐다.

경계할 것 (frontend-design skill의 AI-디폴트 3종 회피):
- **크림 배경 + 고대비 세리프 + 테라코타 액센트 3종 세트 금지** — 이게 AI 기본값 1번이다. 우리는 크림 바탕만 취하고 액센트는 **로스팅 앰버**(#8F5D14)를 승계해 조합을 깬다
- "검정 배경 + 형광 그린 단일 액센트" 금지
- 신문형 hairline 레이아웃 금지
- 파스텔(채도 빠진 분홍·하늘·민트) 금지 — 크림은 파스텔이 아니라 **웜 뉴트럴**이다

## 2. 토큰 (Tailwind config로 구현 — 토큰 밖 임의 값 금지)

### 색 (크림 라이트 단일 테마 — 2026-08-13 전면 개정, "Morning Roastery")

> **왜 다크를 버렸나**: "우울하다 → 너무 어둡다 → 디자인·색상이 별로다" 3회 누적. 글래스모피즘·반계조 상승으로 두 번 대응했지만 근본 원인은 **갈색 위 갈색**이라 위계가 생기지 않는 것이었다. 톤 조절이 아니라 방향 전환으로 해결.
> **왜 테라코타가 아닌가**: frontend-design 스킬이 "크림 배경 + 고대비 세리프 + 테라코타"를 AI 기본값 1순위로 금지한다(아래 §1 무드와도 충돌). 크림 바탕은 취하되 액센트는 **기존 브랜드 자산인 로스트 앰버를 승계**해 고유성을 지킨다.
> **토큰 이름 주의**: 이름은 다크 시절 것을 유지한다(`roast-950`이 이제 가장 밝은 배경). 20여 파일 리네임의 회귀 위험보다 문서화가 낫다고 판단 — 이름이 아니라 아래 역할을 따를 것.
> 모든 값은 WCAG AA(4.5:1) **실측** 통과 조합이다.
| 토큰 | 값 | 용도 |
|---|---|---|
| `roast-950` | #EFE7DA | **페이지 배경** — 웜 크림. 순백(#FFF) 금지 |
| `roast-900` | #FDFAF4 | **카드 내부 서피스** (배경보다 밝게 떠오름) |
| `roast-700` | #DCCDBA | 보더, 분리선 |
| `crema-100` | #2A1C12 | **본문 텍스트** — 에스프레소. 크림 위 13.6:1 |
| `crema-400` | #6B5A48 | 보조 텍스트 — 크림 위 5.35:1 |
| `amber-glow` | #8F5D14 | 액센트(별점·CTA배경·레이더 스트로크). 크림 위 4.54:1이면서, 이 색을 배경으로 쓸 때 얹는 크림 텍스트가 5.5:1 — **양방향 AA**라 단일 토큰으로 텍스트·배경 겸용 |
| `aura` | #D9A24E | 장식 그라데이션 전용 — 텍스트에 쓰지 않는다 |

### 타이포
- Display: **세리프 계열 국문 대응** — 조합 후보: Nanum Myeongjo(무료·안전) 또는 유료 감성 세리프. 히어로 헤드라인·원두 이름에만 제한 사용
- Body: **Pretendard** — UI 전반
- Data/라틴: **JetBrains Mono 또는 유사 모노** — 별점 숫자, 통계, 날짜 (다이어리의 "기록" 정체성 강화)
- 스케일: display 40/48, h2 24, body 15, caption 12. 자간은 국문 -1% 기본

### 형태
- radius 16 (카드) / 999 (칩, 배지). 깊이는 **더블베젤 + 확산 그림자**로 표현 (라이트에선 그림자가 살아난다 — 다크 시절 "그림자 대신 보더" 규칙은 폐기)
- 레이더 차트: amber-glow 스트로크 1.5px + 내부 12% 채움. 앱/공유카드/SEO 페이지 전부 동일 스타일 (브랜드 시각 자산)

### "더블베젤" 서피스 (2026-08-13 — 크림 라이트 전환과 함께 글래스모피즘 대체)

- 카드는 평평한 사각형이 아니라 **"알루미늄 트레이에 앉은 유리판"**: 트레이(6px) → 링 헤어라인(1px) → 내부 광택(inset 1px) → 확산 앰비언트 그림자. 라이트에서 카드가 배경에 묻히던 문제의 해법 (supanova soft-skill §4.A)
- DOM 중첩 없이 `box-shadow` spread 레이어로 그린다 → **레이아웃 영향 0**, 기존 `.glass-card` 클래스명 그대로 유지(20여 파일 리네임 회귀 위험 회피)
- **종이 질감 3.5% 노이즈**(`body::after` 고정 오버레이): 크림 단색이 밋밋해 보이는 것을 막는 핵심 장치. 고정 요소라 스크롤 성능 영향 없음
- **모션 시그니처** `cubic-bezier(.16,1,.3,1)`: 스프링 감. linear·ease-in-out 금지. 진입 애니메이션은 blur(3px)→선명으로 "떠오르는" 물성 부여
- 국문 `word-break: keep-all` 전역 적용 — 어절 단위 줄바꿈
- 하단 내비는 반투명 유리가 아니라 **거의 불투명한 크림**(0.92) — 라이트에서 반투명은 콘텐츠가 비쳐 읽히지 않는다

### (폐기) "Roast Glass" — 글래스모피즘 서피스 (2026-07-10, 다크 시절)
- 진단: 문제는 어둠 자체가 아니라 서피스가 납작했던 것 (roast-900 단색 카드 = 깊이감 없음). 라이트모드 도입은 브랜드 원칙 위반이라 선택지에서 제외, 대신 다크를 유지한 채 깊이·빛을 더하는 방향으로 해결
- 토큰 (globals.css `:root`): `--glass-bg: rgba(28,19,16,.55)`, `--glass-bg-strong: rgba(28,19,16,.72)`(내비 등 상시 노출 서피스용, 가독성 위해 더 불투명), `--glass-border: rgba(241,231,219,.1)`, `--glass-highlight: rgba(255,255,255,.05)`(안쪽 상단 1px — 유리 상단 반사), `--glass-blur: 20px`
- 유틸리티: `.glass-card`/`.glass-card-strong` (`@layer components` — Tailwind 유틸리티가 항상 우선하도록 의도적 배치. 예: `border-dashed`를 같이 주면 `.glass-card`의 solid 보더를 덮어씀)
- 앰비언트 글로우: `body::before`에 고정 위치 radial-gradient 3장(amber-glow·aura 계열, 저채도) — 배경에 은은한 빛 웅덩이를 깔아 서피스들이 그 위에 "떠 있는" 느낌을 준다
- 적용 범위: 리스트/카드형 서피스 전반(체크인 히스토리, 다이어리 통계, 지도 리스트, best/flavor 랭킹, 히어로 최근 체크인, admin 리스트) + 상시 노출 내비(`BottomNav`, glass-bg-strong 사용) + 점선 안내 박스(사진 업로드, 새 원두 등록, admin 안내). 작은 폼 컨트롤(select, input)과 필칩(rounded-full 태그)은 미적용 — 크리스프하게 유지해 탭 대상 명확성 확보
- 근거: ui-ux-pro-max skill의 "Modern Dark (Cinema Mobile)" 스타일 — 다크+글래스모피즘+앰비언트 글로우 조합, "순검정(#000) 회피" 원칙(우리 roast-950 `#120C09`와 이미 부합), `blur 20`/`radius 16`/`hairline border rgba(255,255,255,.08)` 파라미터를 그대로 채택
- 접근성: 텍스트는 전부 crema-100/crema-400이며 유리 서피스 아래 배경이 항상 roast-950 계열 어두운 톤이라 4.5:1 대비 유지 (밝은 배경 위 글래스가 아니므로 대비 리스크 낮음)

### 레이더 노출 규칙 (2026-07-12 추가 — 시그니처를 스스로 가리던 문제 교정)
- **내 기록의 레이더는 1잔부터 연다.** 5건·10건 룰은 *공개 집계*(원두·카페 평균, user_stats)에 적용하는 정책이지 개인 기록에 적용할 근거가 없었다. 과적용 결과 실사용 화면에서 시그니처(레이더)가 한 번도 안 보이는 상태였다
- 대신 **표본 수를 항상 병기**한다("N잔 기준"). 10잔 미만이면 "아직 표본이 적어요" 안내를 덧붙여 과신을 막는다 — 데이터를 숨기는 대신 맥락을 준다
- 사진 없는 체크인은 다이어리·홈 카드의 썸네일 자리를 **레이더가 대신한다**. 사진이 선택이 된 이상(§아래 체크인 규칙) 사진 없는 기록도 시각적으로 비지 않아야 한다

### 모션 규칙 (2026-07-12 추가 — 디자인 폴리시 1차)
- `.pressable`: 탭 가능한 카드·CTA 전부에 부착. active에서 scale 0.98, 150ms ease-out (HIG/MD scale-feedback). hover 없는 터치 환경의 즉각 반응용
- `.stagger-item` + 인라인 `--stagger-i`: 리스트 진입 시 항목당 40ms 지연 fade-up(280ms). 지연 상한은 8번째 항목 — 긴 리스트 아래쪽이 한없이 늦지 않게
- 둘 다 `prefers-reduced-motion: reduce`에서 완전 비활성. 새 모션 추가 시 이 두 유틸리티를 먼저 재사용하고, 새 패턴이 필요하면 여기 등록 후 사용

### 워드마크 로고타입 (2026-07-09 추가 — FAMIMA 레퍼런스 캘리브레이션)
- 서체: **Space Grotesk 700** (라틴 전용 — 국문 본문 Pretendard, 국문 디스플레이 Nanum Myeongjo는 그대로 유지, 워드마크에만 예외 적용)
- 투톤 분리: 워드마크 본체는 crema-100, 마지막 마침표(`.`)만 amber-glow — 국문 말장난("빈.")과도 자연스럽게 맞물림
- 언더라인 바: 워드마크 전체 폭 기준 amber-glow 굵은 바. **브랜드 진입점(히어로, 공유카드, /c 랜딩의 첫 로고)에만 사용** — 내비게이션 등 반복 노출되는 헤더에서는 언더라인 없이 투톤만 (매 화면 볼드하게 반복 노출하면 "볼드함은 한 곳에" 원칙과 충돌)
- 구현: `components/Wordmark.tsx` (웹), `lib/shareCard/render.ts`의 `drawWordmark`(Canvas 공유카드), `app/c/[id]/opengraph-image.tsx`의 `WordmarkOg`(Satori — 폰트 fetch 리스크 회피 위해 기본 폰트에 동일 투톤+언더라인 장치만 이식, 서체 자체는 다름) 3곳에 동일 규칙 구현. 새 터치포인트에 워드마크를 놓을 땐 이 셋 중 맞는 걸 재사용 — 직접 텍스트로 다시 만들지 않는다

## 3. 히어로 랜딩 (/) — 시그니처 스펙

**한 문장 정의: 크림빛 지면 위에서 진한 원두 한 알이 천천히 360° 회전한다. 그게 전부이고, 그래서 우아하다.**

> 크림 전환 후 오히려 대비가 강해졌다 — 원두 포스터가 투명 배경 PNG라 진한 브라운이 밝은 지면 위에서 더 또렷하게 선다. 아우라(#D9A24E)는 아침 햇살 역할.

### 구성 (모바일 기준 1열)
```
[상단 로고 워드마크 — 작게]
[3D 원두 — 뷰포트 높이의 45%, 중앙]
[Display 헤드라인 2줄]
[서브카피 1줄]
[CTA 버튼 1개: "첫 잔 기록하기"]
[하단: 최근 공개 체크인 카드 3장 가로 스크롤 — 살아있는 서비스 증거]
```

### 3D 원두 구현 규칙
- react-three-fiber + drei. 원두 GLB 1개(CC0 모델 소싱 또는 프로시저럴 타원체+노멀맵 — Claude Code가 품질 보고 선택, 판단 근거 커밋에 기록)
- 머티리얼: 로스팅 원두의 반광 표면 (roughness ~0.4, 앰버 rim light 1개 + 상단 soft key light)
- 모션: idle 자동 회전 1회전/14초 (빠르면 싸 보인다). 포인터/자이로에 ±6° 미세 패럴랙스. 스크롤 시 서서히 축소·페이드
- **성능 규칙 (LCP 예산과의 계약)**: 사전 렌더한 원두 포스터 PNG를 즉시 출력 → 3D는 idle 후 lazy 마운트 → 크로스페이드 교체. `prefers-reduced-motion`은 포스터 고정. 저사양 감지 시(디바이스 메모리<4GB) 3D 미마운트
- 대안 경로 (3D 품질이 기준 미달일 때): 36프레임 스프라이트 시퀀스 자동 재생 — 우아함이 확보되면 이것도 합격. "3D였는가"가 아니라 "우아했는가"가 기준

### 카피 (@copywriter 초안, 확정은 승인 게이트에서)
- 헤드라인 방향: "그 한 잔, 기억되게." / "당신이 마신 원두가 당신의 취향이 된다" 계열 — 15단어 이내, AI-speak 금지
- CTA는 기능 서술형: "첫 잔 기록하기" (Sign up 류 금지)

## 4. 레퍼런스 수집 프로토콜 (WORKFLOW.md §4와 동일 — 실행 기록은 여기 하단에)
1. Claude Design: 전반 시안/토큰 초안 생성 → 이 문서 토큰과 대조, 차이 나면 사유 기록 후 이 문서 우선
2. Dribbble / Awwwards: "dark premium", "3D product hero", "coffee brand" 계열 3~5개 — 링크 + 채택 요소 1줄씩 아래 표에 기록. 통째 모방 금지, 방향 캘리브레이션 용도
3. 21st.dev: 스텝퍼/슬라이더/칩 UI 패턴 소견 참조 (체크인 플로우 컴포넌트)

| 레퍼런스 링크 | 채택 요소 | 기각 요소 |
|---|---|---|
| FAMIMA 리브랜딩 (사용자 제공, 2026-07 — 패밀리마트 아자부다이힐즈 매장, Instagram @rei_matsunuma) | 워드마크 투톤 분리 + 굵은 언더라인 바 장치, 볼드 지오메트릭 서체(Space Grotesk로 번역) | 그린+시안 투톤 컬러 자체(우리 팔레트 위반이라 amber-glow로 대체), 밝은 배경·매장 전체 톤(다크 프리미엄 포지셔닝과 상충이라 미채택) |
| Lamborghini 웹 디자인 시스템 (사용자 제공 문서, 2026-07-10) | 순검정 대신 다크 캔버스에 대한 확신 검증용 참조, 풀블리드 히어로·소수 액센트컬러 원칙 재확인 | 보더radius 0(글래스모피즘과 정면 충돌이라 미채택 — 우리는 radius 16 유지), 전면 대문자 슈팅 타이포(브랜드 톤과 안 맞음), 단일 골드 액센트 외 색 절대 금지 규칙(우리는 amber-glow 하나로 이미 동일 원칙 적용 중이라 참고만), 블러 전면 배제 철학(글래스모피즘 요청과 정반대라 미채택) |

## 5. 화면별 우선순위
- **승인 게이트 (아름다워야 함)**: 히어로, 공유카드, /c 랜딩
- **Ugly 허용 (기능 우선)**: admin, 설정, 빈 상태 화면 — 단 토큰은 준수
- 320px 폭 대응 전 화면 필수. 키보드 등장 = 설계 실패 (메모 필드 제외)

# VEANN — WORKFLOW.md (Claude Code Web 운영 프로토콜)

> 이 프로젝트는 Claude Code (web)에서 개발한다. 모든 세션은 이 프로토콜을 따른다.

---

## 1. 리포 초기 세팅 (사용자가 1회 수행)
1. GitHub 새 리포 생성 (private) → Claude Code web에 연결
2. 이 패키지 전체를 리포 루트에 커밋 (CLAUDE.md가 루트에 있어야 Claude Code가 자동 인식)
3. Vercel에서 리포 import → 프레임워크 Next.js 자동 감지 → 환경변수는 Supabase 프로젝트 생성 후 입력
4. Supabase 프로젝트 생성 (리전: ap-northeast-2 서울) → URL/anon key를 Vercel env로

## 2. 세션 시작 프로토콜 (매 세션)
1. **Karpathy 스타일 시작**: 코드를 만지기 전에 문제를 완전히 이해한다. 첫 응답은 항상 ①현재 리포 상태 파악 ②이번 세션의 단일 목표 정의 ③가장 단순한 경로 제시. 추상화를 미리 만들지 않는다 — 3회 반복된 패턴만 추출한다. (skill: karpathy)
2. **Plan 모드 우선**: 구현 전 반드시 plan 모드로 계획 수립 → 사용자 승인 → 실행. 계획에는 (a) 변경 파일 목록 (b) 스키마/스펙 문서 변경 여부 (c) 검증 방법(프리뷰 URL에서 무엇을 확인할지)이 포함되어야 한다.
3. **문서 우선 원칙 유지**: 스펙 변경이 필요하면 TECHNICAL_SPEC.md를 먼저 수정하는 커밋 → 그 다음 코드 커밋.

## 3. 모델 운용 전략
- **계획/아키텍처/디자인 방향 결정**: 최상위 모델(Fable 5)로 plan 모드 진행. 계획이 상세하게 확정되면 —
- **반복 구현/보일러플레이트/컴포넌트 양산**: 더 빠른 모델(Sonnet/Opus 계열)로 전환해 속도 우선 실행.
- 전환 기준: "계획서만 보고 주니어가 구현 가능한 수준"이면 빠른 모델로 넘긴다. 판단이 필요한 지점(스키마 변경, 히어로/공유카드 비주얼, RLS)이 나오면 다시 상위 모델.

## 4. 디자인 파이프라인 (히어로·공유카드·전 화면 공통)
1. **스킬 로드**: 디자인 작업 세션은 `frontend-design` skill + `ui-ux-max-pro` skill을 반드시 로드하고 시작. 두 스킬의 원칙이 충돌하면 frontend-design(고유성) > ui-ux(관례) 순.
2. **레퍼런스 수집 순서**:
   - Claude Design에서 전반 디자인 시안/토큰 초안 생성 → 리포에 스크린샷과 토큰 반영
   - Dribbble + Awwwards에서 "dark premium / coffee / 3D product hero" 계열 레퍼런스 3~5개 수집 (링크와 채택 요소를 DESIGN_DIRECTION.md 하단에 기록 — 모방 아닌 방향 캘리브레이션 용도)
   - 21st.dev에서 컴포넌트 패턴(스텝퍼, 슬라이더, 카드) 소견 참조
3. **디자인 승인 게이트**: 히어로와 공유카드는 Vercel 프리뷰 URL 스크린샷으로 사용자 승인 후 머지. 나머지 화면은 Ugly 허용.
4. 모든 시각 결정은 DESIGN_DIRECTION.md 토큰에서 파생. 토큰 밖 임의 색/폰트 사용 금지.

## 5. 브랜치/커밋 규칙
- main 직접 푸시 금지. 기능 단위 브랜치 → PR → Vercel 프리뷰 확인 → 머지
- 커밋 메시지: `[영역] 요약 — 근거 1줄` (예: `[checkin] IndexedDB 드래프트 추가 — 오프라인 유실 방지`)
- 스키마 변경은 supabase/migrations/ SQL 파일로만 (대시보드 수기 변경 금지 — 재현 불가)

## 6. 세션 종료 프로토콜
- NOTES.md에 3줄 기록: 이번에 한 것 / 막힌 것 / 다음 세션 첫 태스크
- (frontend-design skill 권고사항 반영: 시도한 디자인 방향의 기각 사유도 기록 — 다음 패스에서 재시도 방지)
